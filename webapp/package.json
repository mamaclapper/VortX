{
  "name": "vortx-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "VortX web client (web.vortx.tv). A direct/debrid/HLS-first Stremio client that talks to the Stremio add-on protocol and Cinemeta straight from the browser. No streaming server, no native shell.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=vortx-web"
  },
  "dependencies": {
    "hls.js": "^1.6.16"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.16",
    "wrangler": "^4.20.0"
  }
}
